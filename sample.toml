[param1]
type = "boolean"
description = "the first param"

[param2]
type = "integer"

[param3]
type = "float"

[param4]
type = "datetime"

[param5]
type = "string"

[param6]
type = "file" # TOML string.
optional = true
repeated = true # TOML array.

[param7]
type = "enum" # TOML string.
values = [ "aaa", "bbb", "ccc" ]

# Default values are the zero values for each type (false, 0, 0.0, "", epoch, absent).

# TODO: Mapping to command line flags.
# TODO: Positional params.
# TODO: Enums generated by running other commands (e.g. `git branch`).
# TODO: File patterns.
